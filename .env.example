# Get a key from https://aistudio.google.com/apikey
# Image models (gemini-2.5-flash-image) often need a project with image quota /
# billing — free tier can return 429 with limit: 0 even after a few requests.
# Check: https://aistudio.google.com/rate-limit
EXPO_PUBLIC_GEMINI_API_KEY=
